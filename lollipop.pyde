def setup():
    size(200, 300) # set the window size
    whitecolor = 255
    background(whitecolor, 0, whitecolor) # set backgroundcolor
    fill(0, whitecolor, whitecolor)
    ellipse(100, 150, 60, 60)
    fill(whitecolor, whitecolor, whitecolor)
    rect(97.5, 180, 5, 80)
    fill(250,0, 0)
    rect(100, 180 , 5, 80)
    fill(whitecolor, whitecolor, whitecolor)
    rect(103, 180, 5, 80)
